# ink-blob
A responsive website where every keypress generates a unique, evolving inkblot pattern using procedural generation and canvas physics — each blot morphs based on key type, timing, and pressure (via keyboard repeat rate), and clusters into surreal shapes that drift and interact like sentient ink in water.
